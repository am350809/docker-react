language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t am350809/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true am350809/docker-react npm run test
